export function calculateInventorySummary(input: {
  totalMaterials: number;
  lowStockCount: number;
  totalStockValue: number;
  reservedValue: number;
  materialsBelowMinimum: unknown[];
  recentMovements: unknown[];
}) {
  return input;
}
